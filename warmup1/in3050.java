public class in3050{
public boolean in3050(int a, int b) {
  boolean in30s = (a >= 30 && a <= 40) && (b >= 30 && b <= 40);
  boolean in40s = (a >= 40 && a <= 50) && (b >= 40 && b <= 50);
   return in30s || in40s;
}
}